# PyNN spike-based learning abstraction

Goal: backend-agnostic interface for spike-based BP (e-prop, EventProp).

Backends:
- NEST (e-prop)
- mlGeNN (e-prop, EventProp)
- Optional: SpiNNaker2 (architecture note)

Deliverables:
- Runnable examples in `examples/`
- Mapping docs in `docs/`